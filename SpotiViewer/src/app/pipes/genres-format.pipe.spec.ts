import { GenresFormatPipe } from './genres-format.pipe';

describe('GenresFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new GenresFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
